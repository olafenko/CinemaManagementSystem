<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:PDABProjekt.Views">


    <Style TargetType="{x:Type local:WszystkieViewBase}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:WszystkieViewBase}">
                    <Grid Margin="0,10,0,10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Left">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="Dodaj" Height="30" Width="70" Command="{Binding AddCommand}" Background="#2A2B2E" Foreground="White" BorderBrush="#4A90E2"  FontWeight="SemiBold" BorderThickness="2"/>
                            <Button Grid.Column="1" Content="Edytuj" Height="30" Width="70" Background="#2A2B2E" Foreground="White" BorderBrush="#4A90E2"  FontWeight="SemiBold" BorderThickness="2"/>
                            <Button Grid.Column="2" Content="Usun" Height="30" Width="70" Background="#2A2B2E" Foreground="White" BorderBrush="#4A90E2"  FontWeight="SemiBold" BorderThickness="2"/>
                            <Button Grid.Column="3" Content="Odswiez" Height="30" Width="70" Command="{Binding LoadCommand}" Background="#2A2B2E" Foreground="White" BorderBrush="#4A90E2"  FontWeight="SemiBold" BorderThickness="2"/>

                        </Grid>
                        <Expander Grid.Row="1" Header="Filtrowanie i sortowanie" Background="#2A2B2E" BorderBrush="#4A90E2" Foreground="White" FontWeight="SemiBold" Padding="3,3,3,3">
                            <Grid Margin="5,5,5,5">
                                <Grid.RowDefinitions>
                                    <RowDefinition  Height="25"/>
                                    <RowDefinition  Height="25"/>
                                    <RowDefinition  Height="30"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition  Width="230"/>
                                    <ColumnDefinition  Width="60"/>
                                    <ColumnDefinition  Width="230"/>
                                </Grid.ColumnDefinitions>
                                <Label  Grid.Row="0"  Grid.Column="0" Content="Sortowanie:  " Foreground="White"/>
                                <ComboBox  Grid.Row="1"  Grid.Column="0" ItemsSource="{Binding  SortComboBoxItems}"  SelectedValue="{Binding Path=SortField,Mode=TwoWay}"  Margin="0,0,60,0"/>
                                <Button  Content="Sortuj"  Grid.Row="1"  Grid.Column="0" Command="{Binding  SortCommand}" Margin="170,0,0,0"/>
                                <Label  Grid.Row="0"  Grid.Column="2" Content="Wyszukiwanie:  " Foreground="White"/>
                                <ComboBox  Grid.Row="1"  Grid.Column="2" ItemsSource="{Binding  FindComboBoxItems}"  SelectedValue="{Binding Path=FindField,Mode=TwoWay}"  />
                                <TextBox  Grid.Row="2"  Grid.Column="2" Margin="0,5,60,0"  Text="{Binding  FindTextBox}"/>
                                <Button  Content="Szukaj"  Grid.Row="3"  Grid.Column="2" Command="{Binding  FindCommand}" Margin="170,5,0,0"/>
                            </Grid>
                        
                        </Expander>
                        <ContentPresenter Grid.Row="2" Margin="0,5,0,5"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:JedenViewBase}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:JedenViewBase}">
                    <Grid Margin="0,10,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Left">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="Zapisz i zamknij" Height="30" Width="120" Margin="10,0" Command="{Binding  SaveAndCloseCommand}" Background="#2A2B2E" Foreground="White" BorderBrush="#4A90E2"  FontWeight="SemiBold" BorderThickness="2"/>
                            <Button Grid.Column="1" Content="Anuluj" Height="30" Width="70"  Background="#2A2B2E" Foreground="White" BorderBrush="#4A90E2"  FontWeight="SemiBold" BorderThickness="2"/>
                        </Grid>
                        <ContentPresenter Grid.Row="1" Margin="0,5,0,5"></ContentPresenter>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:RaportViewBase}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:RaportViewBase}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,5,5,5">
                            <Button  Content="Eksportuj do CSV" Command="{Binding ExportCommand}" Height="30" Width="auto" Padding="2,0,2,0" Background="#2A2B2E" Foreground="White" BorderBrush="#4A90E2"  FontWeight="SemiBold" BorderThickness="2"/>
                        </StackPanel>
                        <Separator Grid.Row="1"/>
                        <ContentPresenter Grid.Row="2"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
