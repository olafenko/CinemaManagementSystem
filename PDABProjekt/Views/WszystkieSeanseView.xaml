<local:WszystkieViewBase x:Class="PDABProjekt.Views.WszystkieSeanseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PDABProjekt.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}" CanUserAddRows="false">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Tytuł filmu" Binding="{Binding TytulFilmu}"/>
                <DataGridTextColumn Header="Numer sali" Binding="{Binding Sala}"/>
                <DataGridTextColumn Header="Liczba miejsc" Binding="{Binding LiczbaMiejsc}"/>
                <DataGridTextColumn Header="Liczba wolnych miejsc" Binding="{Binding WolneMiejsca}"/>
                <DataGridTextColumn Header="Typ sali" Binding="{Binding TypSali}"/>
                <DataGridTextColumn Header="Data" Binding="{Binding DataSeansu}"/>
                <DataGridTextColumn Header="Od" Binding="{Binding GodzinaOd}"/>
                <DataGridTextColumn Header="Do" Binding="{Binding GodzinaDo}"/>
                <DataGridTextColumn Header="Czas trwania (min)" Binding="{Binding CzasTrwania}"/>
                <DataGridTextColumn Header="Język seansu" Binding="{Binding JezykWersji}"/>
                <DataGridTextColumn Header="Wersja jezykowa" Binding="{Binding WersjaJezykowa}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>
                
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
