<local:JedenViewBase x:Class="PDABProjekt.Views.AddDirectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PDABProjekt.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Padding="10" Background="#202123">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Column="0" Grid.Row="0" Content="Imie: " Margin="0,10" FontWeight="Bold" Foreground="#f0f0f0"/>
            <TextBox Grid.Column="1" Grid.Row="0" Margin="0,10" Foreground="Black" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Column="0" Grid.Row="1" Content="Nazwisko " Margin="0,10" FontWeight="Bold" Foreground="#f0f0f0"/>
            <TextBox Grid.Column="1" Grid.Row="1" Margin="0,10" Foreground="Black" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Column="0" Grid.Row="2" Content="Kraj: " Margin="0,10" FontWeight="Bold" Foreground="White"/>
            <ComboBox  Grid.Column="1" Grid.Row="2" Margin="0,10" Foreground="Black"
              ItemsSource="{Binding Countries}"
              DisplayMemberPath="Nazwa"
              SelectedValuePath="IdKraju"
              SelectedValue="{Binding CountryId, Mode=TwoWay}"/>


            

        </Grid>
    </Border>
</local:JedenViewBase>
